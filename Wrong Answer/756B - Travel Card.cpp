#include <bits/stdc++.h>
using namespace std;
typedef long long ll;
ll time[100005],cost;
int main() {
	ios_base::sync_with_stdio(0);
	int n;
	ll start=0;
	cin >> n;
	for (int i = 1; i <= n; i++) {
		cin >> time[i];
		time[i]+=time[i-1];
		cost+=20;
		if cost
	}
}
